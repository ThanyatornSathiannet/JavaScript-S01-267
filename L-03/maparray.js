const finalParticipants = ["Taylor","Donald","Don","Natasha","Bobby"];

const announcments = finalParticipants.map((member) => {
    return member + " join the contest.";
});

console.log(announcments);